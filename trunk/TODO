bash script (make/launch script)
resources folder
system sketch

-svn commands:
up, st, ci, co, add

-write a factory for activate (commands)

-write a class to handle controlled files (svn - is it a dir? added? etc)